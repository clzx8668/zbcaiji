# 招标/中标公告采集工具 — 运行说明

本项目用于定时采集指定关键词的招标/中标公告。当前中国政府采购网可正常采集；山东政府采购网（API 直连）正在适配；乙方宝因详情页强制微信扫码登录、无法无人值守，已停用。本文档覆盖：环境准备、Web 管理后台、定时调度、手动爬取、独立测试、停止方式与常见问题。

> 所有命令默认在项目根目录 `E:\Dev\zbcaiji` 下执行；下文 `python` 指项目使用的 Python 3.12 环境（若不在 PATH 中，请使用虚拟环境路径或 `py -3.12` 代替）。

---

## 1. 环境准备（首次部署）

```powershell
cd E:\Dev\zbcaiji

# 1) 安装依赖
python -m pip install -r requirements.txt

# 2) 安装 Playwright 浏览器（必须）
python -m playwright install chromium

# 3) 复制并填写环境变量
Copy-Item .env.example .env
#   然后编辑 .env，至少填写 LLM_API_KEY（DeepSeek）

# 4) 验证环境
python debug_test.py
```

如果使用 Camoufox 引擎（反检测更强），另需：

```powershell
python -m pip install camoufox
python -m camoufox init
```

---

## 2. 配置文件 `.env`

常用配置项：

| 变量 | 说明 | 默认值 |
| --- | --- | --- |
| `LLM_API_KEY` | DeepSeek / OpenAI 兼容 API Key | 空（不填则 AI 提取关闭） |
| `LLM_API_BASE` | API 地址 | `https://api.deepseek.com/v1` |
| `LLM_MODEL` | 模型名 | `deepseek-chat` |
| `BROWSER_ENGINE` | 浏览器引擎 `playwright` / `camoufox` | `playwright` |
| `CRAWL_TIMEOUT` | 单次页面加载超时（秒） | `120` |
| `CRAWL_MAX_SECONDS` | **单个站点单次任务总时长上限（秒），超时自动停止** | `900` |
| `MAX_PAGES` | 结果列表最大翻页数 | `5` |
| `DATABASE_PATH` | SQLite 数据库路径 | `data/bid_scraper.db` |
| `WEB_ADMIN_USER` / `WEB_ADMIN_PASS` | Web 后台登录账号 / 密码 | `admin` / `admin123` |
| `FLASK_SECRET_KEY` | Flask 密钥（生产环境务必修改） | 默认占位字符串 |
| `DINGTALK_WEBHOOK` / `WECOM_WEBHOOK` | 钉钉 / 企业微信机器人通知（可选） | 空 |

> 注意：`LLM_API_KEY` 是敏感信息，请勿提交到任何代码仓库。

---

## 3. Web 管理后台（推荐日常使用）

### 启动

```powershell
python run.py web
```

访问地址：<http://localhost:5000/admin>
默认账号：`admin` / `admin123`（可在 `.env` 的 `WEB_ADMIN_USER` / `WEB_ADMIN_PASS` 中修改）

常用参数：

```powershell
python run.py web --port 8080          # 修改端口
python run.py web --host 127.0.0.1     # 只允许本机访问
python run.py web --debug              # 调试模式（不推荐长时间运行）
```

后台功能：

- **定时任务**：启动/停止调度器、查看下次运行时间；
- **立即执行**：对指定站点手动触发一次爬取（在后台线程运行，非交互、不会阻塞页面）；
- **站点配置**：新增/修改/停用站点、Excel 导入导出；
- **数据管理**：公告列表、搜索、归档、批量操作、任务记录、日志查看。

### 停止

在启动 Web 的终端按 `Ctrl + C` 即可。若是在后台启动的（如计划任务），见第 7 节“停止所有服务”。

---

## 4. 定时调度

### 启动

```powershell
python run.py schedule
```

调度器按每个站点在配置中的 `cron_expr`（默认每天 9:00）自动执行爬取。

调度行为说明（v1.0 加固后）：

- 后台调度**强制非交互**：遇到验证码/登录等需要人工介入的情况会自动跳过并记录，不会卡死等待输入；
- **全局串行**：任意时刻只有一个爬虫在运行，避免并发导致站点频控；
- 单站任务有**总时长上限**（`CRAWL_MAX_SECONDS`，默认 900 秒），到点自动停止。

### 停止

```powershell
# 在终端中按 Ctrl + C
```

> 建议：被站点封 IP 的站点可先在 Web 后台“停用”，解封后再启用，避免每天空跑。

---

## 5. 手动爬取

```powershell
# 爬取全部启用的站点（串行执行）
python run.py crawl --all

# 只爬指定站点（站点名需与配置完全一致）
python run.py crawl --site "中国政府采购网"
python run.py crawl --site "乙方宝"
python run.py crawl --site "山东政府采购网"

# 强制重新分析搜索入口（忽略缓存，改版后使用）
python run.py crawl --site "中国政府采购网" --reanalyze

# 指定浏览器引擎
python run.py crawl --site "中国政府采购网" --engine camoufox

# 非交互模式（遇到验证码/登录自动跳过，适合脚本/计划任务）
python run.py crawl --site "乙方宝" --no-interact
```

示例输出：

```text
爬取完成: 1 个站点, 30 条结果, 10 条新增, 0 条变更
```

说明：

- 每次爬取只使用配置里的**第一个关键词**搜索，其余关键词留给后续结果过滤（避免 AND 搜索导致结果过少）；
- 手动执行默认是**交互模式**：打开可见浏览器窗口，遇到微信扫码登录等需要人工介入的场景会弹出窗口提示，扫码后在终端按 Enter 继续；
- 无人值守环境（定时调度、Web 后台“立即执行”）始终使用**无头模式**，遇到登录自动跳过并记录，不会卡住；如需给某站点扫码登录，先手动跑一次该站点即可，会话会保存并复用。

---

## 6. 其他独立命令

### 生成 / 导入站点配置 Excel

```powershell
# 生成模板（仅当模板不存在时）
python run.py init

# 从 Excel 导入站点配置（--replace 表示先清空再导入）
python run.py seed --file config/template.xlsx
python run.py seed --file 我的站点.xlsx --replace
```

### 查看统计

```powershell
# 最近 7 天统计（支持 30d 等）
python run.py report
python run.py report --last 30d
python run.py report --site "中国政府采购网" --type 招标公告
```

### 导出数据

```powershell
# 导出 Excel / JSON
python run.py export --format excel
python run.py export --format json

# 按站点 / 日期 / 类型筛选
python run.py export --format excel --site "乙方宝"
python run.py export --format excel --from 2026-08-01 --to 2026-08-12
python run.py export --format excel --type 中标公告

# 只导出最近 7 天新增
python run.py export --format excel --new

# 指定输出目录
python run.py export --format excel --output D:\exports
```

### 交互式登录（微信扫码等）

```powershell
python run.py login --site "中国政府采购网"
```

登录会话会保存到 `data/browser_sessions/`，下次爬取自动加载。

---

## 7. 停止所有服务（Windows）

Web 和调度器在各自终端里按 `Ctrl + C` 即可。如果是在后台/计划任务中启动、找不到窗口，可用以下命令按命令行特征定位并结束（**先确认进程列表再结束，避免误杀**）：

```powershell
# 1) 查看正在运行的 python 进程及命令行
Get-CimInstance Win32_Process -Filter "Name like 'python%'" |
  Select-Object ProcessId, CommandLine

# 2) 只结束本项目的 run.py 进程
Get-CimInstance Win32_Process -Filter "Name like 'python%'" |
  Where-Object { $_.CommandLine -like '*zbcaiji*run.py*' } |
  ForEach-Object { Stop-Process -Id $_.ProcessId }
```

> 谨慎使用 `Stop-Process`：请先执行第 1 步确认 PID 和命令行，不要直接 `Get-Process python | Stop-Process`。

---

## 8. 单独测试 / 调试

### 自检脚本（不依赖站点网络）

```powershell
python debug_test.py
```

依次测试 4 项：

1. **Playwright 浏览器**：启动浏览器并访问 `httpbin.org`（需要网络）；
2. **内容提取器**：用内置样例 HTML 测试 CSS 提取；
3. **SQLite 存储**：插入/重复插入/统计（会向真实数据库写入一条“测试站点”记录）；
4. **Excel 读取**：读取 `config/template.xlsx` 站点配置。

### 山东站 API 探测脚本

```powershell
python debug_api.py
```

用 Playwright 打开山东政府采购网，探测 `searchAllByCode` 接口的各种参数组合返回（需要网络，且当前该站对项目出口 IP 有限制，被封时返回 400/blackIp）。

### 只测某个站点完整流程

```powershell
python run.py crawl --site "乙方宝"
```

抓取失败时排查材料：

| 位置 | 内容 |
| --- | --- |
| `data/logs/scraper_*.log` | 运行日志（含每一步耗时与结果数） |
| `data/logs/error_*.log` | 错误日志（如 `Cannot navigate to invalid URL`） |
| `data/debug/` | 列表/详情解析失败的原始 HTML（便于人工排查结构） |
| `data/blocked_sites.json` | 被熔断/跳过的站点及原因 |
| `data/backups/` | 数据库自动备份（Web 启动后每天 02:00 自动备份） |

---

## 9. 常见问题速查

| 现象 | 原因 / 处理 |
| --- | --- |
| 任务一直显示“running” | 旧版本后台任务会在验证码处阻塞；已修复：后台强制非交互 + 总时长上限。历史卡死任务已在 2026-08-12 清理 |
| 日志报 `Cannot navigate to invalid URL ... "陶瓷膜"` | 站点配置把关键词误填到了 `search_url` 字段；现在会直接报配置错误并标记任务失败 |
| 中国政府采购网返回“访问过于频繁” | 出口 IP 被频控/拉黑，等待解封；期间可在 Web 后台停用该站点 |
| 山东政府采购网返回 `blackIp` | 同上的 IP 黑名单；该站搜索走 `:8087` API + 验证码，属 P1 待适配范围 |
| 搜索无结果且连续 3 页为空 | 触发熔断终止；检查搜索是否生效、是否有验证码/WAF |
| 结果里混入 `.pdf`/`.zip` 附件 | 详情页提取过宽导致；当前已清理存量数据，源头收窄在后续优化中 |

---

## 10. 命令速查表

| 用途 | 命令 |
| --- | --- |
| 启动 Web 后台 | `python run.py web` |
| 启动定时调度 | `python run.py schedule` |
| 爬全部站点 | `python run.py crawl --all` |
| 爬单个站点 | `python run.py crawl --site "站点名"` |
| 查看统计 | `python run.py report` |
| 导出 Excel | `python run.py export --format excel` |
| 导出 JSON | `python run.py export --format json` |
| 生成配置模板 | `python run.py init` |
| 导入配置 | `python run.py seed --file 文件.xlsx` |
| 交互登录 | `python run.py login --site "站点名"` |
| 环境自检 | `python debug_test.py` |
| 山东站 API 探测 | `python debug_api.py` |
